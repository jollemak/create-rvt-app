# create-rvt-app

🚀 One-command setup for **React + Vite + Tailwind v4**

## Usage

### Default (TypeScript)

```bash
npx create-rvt-app my-app
```

### JavaScript version

```bash
npx create-rvt-app my-app --js
```

Then:

```bash
cd my-app
npm run dev
```

## Features

* ⚡ React + Vite setup
* 🎨 TailwindCSS v4 (modern `@tailwindcss/postcss` method)
* 🧠 Choose between TypeScript (default) or JavaScript (`--js` flag)

## Development (for maintainers)

To test locally:

```bash
git clone <this-repo>
cd create-rvt
npm install -g .
```

Then run:

```bash
create-rvt-app my-app
```

## License

MIT © 2025

